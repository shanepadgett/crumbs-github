/**
 * Terminal Clear Extension
 *
 * What it does: clears terminal viewport and scrollback when Pi starts and exits.
 * How to use it: enable extension and reload Pi.
 * Example: `/reload`
 */

export { default } from "./src/extension.js";
