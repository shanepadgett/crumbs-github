/**
 * Repo Scaffold Extension
 *
 * What it does: adds `/scaffold` to generate deterministic repo tooling, tasks, and quiet-validator wiring.
 * How to use it: run `/scaffold`, `/scaffold ts markdown`, `/scaffold doctor`, or `/scaffold refine`.
 * Example: `/scaffold ts markdown`
 */

export { default } from "./src/extension.js";
