/**
 * Commit Extension
 *
 * What it does: adds `/commit` to create semantic git commits from deterministic git evidence.
 * How to use it: run `/commit` inside a git worktree with local changes.
 * Example: `/commit`
 */

export { default } from "./src/extension.js";
