/**
 * Crumbs Doctor Extension
 *
 * What it does: adds `/crumbs` utilities to inspect and initialize crumbs config.
 * How to use it: run `/crumbs doctor`, `/crumbs init`, or `/crumbs schema`.
 * Example: `/crumbs schema`
 */

export { default } from "./src/extension.js";
