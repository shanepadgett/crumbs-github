---
description: Implement change through scout → planner → worker subagents
---

Use `subagent` tool.

Run chain:

1. `scout` inspect only files and constraints relevant to request.
2. `planner` turn scout output into smallest safe plan.
3. `worker` implement plan.

Keep scope tight. No side quests.

User request:
$@
